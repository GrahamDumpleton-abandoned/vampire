[Settings]

# Define any variables here which should be available
# within any PSP page under this directory. Any of the
# default variables such as "__config_root__" will also
# be available.

contact = grahamd@dscpl.com.au 

[Handlers]

# Location of the default content handlers. This is only
# consulted when the "VampireDefaultHandlers" option is
# set to "On" using the "PythonOption" setting in the
# "httpd.conf" or ".htaccess" files.

handler_html = %(__config_root__)s/_handler.py
handler_psp = %(__config_root__)s/_handler.py
